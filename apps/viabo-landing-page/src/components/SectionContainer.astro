---
import { twMerge } from "tailwind-merge"

const { class: className, id } = Astro.props
---

<section
	id={id}
	data-section={id}
	class:list={twMerge("section container mx-auto w-full max-w-screen-lg scroll-m-20", className)}
>
	<slot />
</section>
